from setuptools import setup

setup(name='kamerie',
      version='0.1',
      url='https://github.com/kamerie/kamerie-server',
      license='Apache 2.0',
    #   packages=[''],
      install_requires=['pyzmq'],
      zip_safe=False)
